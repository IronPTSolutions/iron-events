import { Component } from 'react';

class EventDetail extends Component {

  // This component must receive the event identifier from props.

  // Iteration 2: retrieve the 'id' event from API and render it.
  // remember, you need to use the events' service after the first time that the component was rendered.

  render() {
    return (<div></div>)
  }
}

export default EventDetail;
